name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    name: Test and build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: dom, fileinfo, gd, mbstring, openssl, pdo_sqlite, sqlite3, xml, zip
          coverage: none

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Cache Composer dependencies
        uses: actions/cache@v4
        with:
          path: vendor
          key: ${{ runner.os }}-composer-${{ hashFiles('composer.lock') }}
          restore-keys: ${{ runner.os }}-composer-

      - name: Install PHP dependencies
        run: composer install --no-interaction --prefer-dist --no-progress --no-scripts

      - name: Prepare Laravel environment
        run: |
          cp .env.example .env
          php artisan key:generate --ansi
          touch database/database.sqlite
          php artisan package:discover --ansi

      - name: Install Node dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build

      - name: Prepare e2e database
        run: |
          touch database/e2e.sqlite
          DB_CONNECTION=sqlite DB_DATABASE=database/e2e.sqlite php artisan migrate --force
          DB_CONNECTION=sqlite DB_DATABASE=database/e2e.sqlite php artisan db:seed --force

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium

      - name: Run PHPUnit
        run: vendor/bin/phpunit --do-not-cache-result

      - name: Run frontend unit tests
        run: npm run test:unit

      - name: Run admin browser smoke tests
        run: npm run test:e2e

      - name: Upload Playwright failure artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-artifacts
          path: test-results/
          if-no-files-found: ignore
